#include <iostream>

using namespace std;

int main() {
        ios::sync_with_stdio(false);

        int x, y;
        cin >> x >> y; 
        
        cout << "SOMA = " << x + y << endl;
        
        return 0;
}
